import checkmark from '@local/assets/checkmark.svg';
import Icon from '@local/components/Icon/Icon';
import React from 'react';

function CheckmarkIcon(props: any) {
  return <Icon glyph={checkmark} {...props} />;
}

export default CheckmarkIcon;
