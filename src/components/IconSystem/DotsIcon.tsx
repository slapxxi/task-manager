import dots from '@local/assets/dots.svg';
import Icon from '@local/components/Icon/Icon';
import React from 'react';

function DotsIcon(props: any) {
  return <Icon glyph={dots} {...props} />;
}

export default DotsIcon;
