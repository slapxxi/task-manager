import cog from '@local/assets/cog.svg';
import Icon from '@local/components/Icon/Icon';
import React from 'react';

function CogIcon(props: any) {
  return <Icon glyph={cog} {...props} />;
}

export default CogIcon;
