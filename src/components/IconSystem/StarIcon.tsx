import star from '@local/assets/star.svg';
import Icon from '@local/components/Icon/Icon';
import React from 'react';

function StarIcon(props: any) {
  return <Icon glyph={star} {...props} />;
}

export default StarIcon;
