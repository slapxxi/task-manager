import cloud from '@local/assets/cloud.svg';
import Icon from '@local/components/Icon/Icon';
import React from 'react';

function CloudIcon(props: any) {
  return <Icon glyph={cloud} {...props} />;
}

export default CloudIcon;
