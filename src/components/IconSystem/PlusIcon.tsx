import plus from '@local/assets/plus.svg';
import Icon from '@local/components/Icon/Icon';
import React from 'react';

function PlusIcon(props: any) {
  return <Icon glyph={plus} {...props} />;
}

export default PlusIcon;
