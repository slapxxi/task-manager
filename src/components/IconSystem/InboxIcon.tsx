import inbox from '@local/assets/inbox.svg';
import Icon from '@local/components/Icon/Icon';
import React from 'react';

function InboxIcon(props: any) {
  return <Icon glyph={inbox} {...props} />;
}

export default InboxIcon;
