import search from '@local/assets/search.svg';
import Icon from '@local/components/Icon/Icon';
import React from 'react';

function SearchIcon(props: any) {
  return <Icon glyph={search} {...props} />;
}

export default SearchIcon;
