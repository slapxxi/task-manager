import list from '@local/assets/list.svg';
import Icon from '@local/components/Icon/Icon';
import React from 'react';

function ListIcon(props: any) {
  return <Icon glyph={list} {...props} />;
}

export default ListIcon;
