import arrowUp from '@local/assets/arrow_up.svg';
import Icon from '@local/components/Icon/Icon';
import React from 'react';

function ArrowUpIcon(props: any) {
  return <Icon glyph={arrowUp} {...props} />;
}

export default ArrowUpIcon;
