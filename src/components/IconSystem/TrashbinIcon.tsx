import trashbin from '@local/assets/trashbin.svg';
import Icon from '@local/components/Icon/Icon';
import React from 'react';

function TrashbinIcon(props: any) {
  return <Icon glyph={trashbin} {...props} />;
}

export default TrashbinIcon;
