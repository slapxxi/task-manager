import bell from '@local/assets/bell.svg';
import Icon from '@local/components/Icon/Icon';
import React from 'react';

function BellIcon(props: any) {
  return <Icon glyph={bell} {...props} />;
}

export default BellIcon;
